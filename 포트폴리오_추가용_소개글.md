# 플랜두씨 다이어리 — 포트폴리오 소개글

**플랜두씨 다이어리**는 계획(Plan) → 실행(Do) → 회고(See)를 하나의 흐름으로 이어주는 개인용 웹 다이어리입니다. 단순히 할 일에 체크만 하고 끝나는 도구가 아니라, "내가 계획을 어느 쪽으로 자주 틀리는지"를 스스로 눈으로 확인하고 다음 계획에 반영하도록 설계했습니다.

- **기술 스택**: 순수 HTML/CSS/JavaScript(ESM) + Supabase(Postgres). 별도 백엔드 서버 없이 Row Level Security로 데이터를 안전하게 보호하면서 브라우저에서 직접 데이터베이스에 접근하도록 구성했습니다.
- **핵심 설계 포인트**:
  - 계획을 수정해도 원래 값이 사라지지 않도록, DB 트리거로 수정 이력을 자동 보존.
  - "완료" 버튼을 연달아 눌러도 기록이 중복되지 않도록, 클라이언트 방어가 아닌 Postgres 함수의 행 잠금으로 서버측에서 idempotency를 보장.
  - 회고 화면의 모든 집계 숫자는 클릭하면 그 숫자를 구성한 실제 기록으로 드릴다운되어, "숫자만 보여주는" 화면이 되지 않도록 함.
  - 화면을 역할별로 5개 페이지(계획/할 일/실행 기록/회고/내보내기)로 분리해 기능이 늘어나도 가독성을 유지.
- **배운 점**: "두 번 눌러도 한 번만 반영" 같은 요구사항은 UI 레벨 방어만으로는 불충분하며, 트랜잭션과 행 잠금 같은 데이터베이스 레벨의 보장이 필요하다는 점을 직접 구현하며 확인했습니다.
