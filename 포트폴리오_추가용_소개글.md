# 포트폴리오 소개글 — 플랜두씨 다이어리

## 한 줄 소개
계획한 하루와 실제로 보낸 하루를 같은 화면에서 비교하며 기록하는 개인 다이어리 웹앱.

## 소개글 (짧은 버전)

**플랜두씨 다이어리**는 습관, 운동, 학습 시간처럼 매일 반복되는 항목을 스스로 정해 기록하고, 계획한 값과 실제 값을 나란히 남길 수 있는 개인 기록기입니다. 순수 HTML/CSS/JS로 만들었고 별도 서버 없이 브라우저 저장소만으로 동작하며, 데이터를 JSON으로 내보내고 다시 불러올 수 있습니다. 기록 구조가 나중에 바뀌어도 기존 기록을 잃지 않도록 자동 마이그레이션을 구현했고, 요일 경계나 잘못된 입력값에도 주간 집계가 흔들리지 않도록 검증 로직을 따로 두었습니다.

## 소개글 (긴 버전)

개인 기록 도구를 직접 써보면서 느꼈던 두 가지 불편에서 출발했습니다. 하나는 "계획"과 "실제"를 따로 관리하다 보면 비교가 번거롭다는 점, 다른 하나는 기록 앱의 데이터 구조가 나중에 바뀌면 예전 기록이 깨지거나 사라진다는 점이었습니다.

이 프로젝트에서는 각 기록에 날짜·시간대·항목·값·단위와 함께 "계획"/"실제" 같은 태그를 붙일 수 있게 해 하나의 목록 안에서 바로 비교할 수 있도록 했습니다. 저장 형식에는 버전을 부여해, 나중에 필드가 추가되어도 예전 형식의 기록을 자동으로 새 형식으로 변환하면서 값을 잃지 않도록 만들었습니다. 이 변환 로직은 여러 번 실행해도 안전하게 동작하는지 별도로 테스트했습니다.

주간 요약 기능에서는 날짜 경계(월요일 시작)와 잘못된 입력값(존재하지 않는 날짜, 숫자가 아닌 값)을 구분해 처리하는 데 신경 썼습니다. 특히 JavaScript의 `Date` 객체가 존재하지 않는 날짜를 조용히 다음 달로 보정해버리는 특성 때문에, 단순 파싱만으로는 잘못된 입력을 걸러낼 수 없다는 점을 발견하고 별도의 재검증 로직을 추가했습니다.

모든 데이터는 브라우저의 `localStorage`에만 저장되어 서버로 전송되지 않으며, JSON 내보내기/가져오기로 기기 간 이동이나 백업이 가능합니다.

## 사용 기술
HTML, CSS, JavaScript (바닐라, 프레임워크 없음), `localStorage`, GitHub Pages

## 링크
- 저장소: (배포 후 반영 예정)
- 배포 주소: (배포 후 반영 예정)

---

## T07 갱신 — 인증 붙이기

이후 Supabase Auth(이메일+비밀번호)로 가입·로그인·로그아웃을 붙이고, 저장소를 `localStorage`에서 Supabase Postgres로 옮겼습니다. 특히 "다른 사용자의 자료가 안 보이는 것"을 화면 로직이 아니라 데이터베이스의 행 단위 보안(Row Level Security) 정책으로 강제해, 요청을 조작해도 서버가 소유자를 확인하도록 만들었습니다. curl로 로그인 상태/로그아웃 후 응답을 직접 비교하고, 계정 두 개를 만들어 서로의 자료에 접근이 실제로 막히는지 검증하는 절차(`verify.sh`)도 함께 만들었습니다.

- 사용 기술(추가): Supabase(Auth, Postgres, Row Level Security), Vercel
- 배포 주소: https://plandosee-noi8.vercel.app/
- 저장소: https://github.com/kjo35861/plan-do-see-Diary
