# 작업내역 체크리스트 — 플랜두씨 다이어리 (T06)

> 모든 AI 모델(Claude, ChatGPT 등)의 공용 인수인계 파일. 새 세션을 시작하면 이 파일을 가장 먼저 읽는다.

## 현재 상태 요약

- 카드 1~4 기능 구현 완료 (기록 CRUD, 저장/내보내기/가져오기/전체삭제, v1→v2 마이그레이션, 주간 집계+오류값 제외)
- 카드 5(5일 실사용 후 개선)는 아직 시작 전 — 실제 날짜 5일에 걸쳐 사용해야 함
- GitHub 배포는 아직 안 함 (로컬/압축본 상태)

## 다음에 할 일

- [ ] 새 GitHub 저장소 생성 후 push, GitHub Pages 배포
- [ ] 배포 주소 확정 후 `검증안내서.md`와 `docs/05_배포.md`에 실제 URL 반영
- [ ] 카드 1일차 사용 시작 (아래 "카드 5 — 5일 사용 기록" 표에 기록)
- [ ] 5일치 사용 뒤 반복된 불편 1건 선정, 수정, `트러블슈팅.md`에 기록

## 결정 기록

| 날짜 | 결정 | 이유 |
|---|---|---|
| 2026-09-01 | 순수 HTML/CSS/JS, 빌드 도구 없음 | 이전 T03·T04 과제와 동일한 방식, GitHub Pages 배포가 가장 단순함 |
| 2026-09-01 | 저장소를 `localStorage` 단일 키(`plandusy_diary_data`)로 관리 | 실제 백엔드 없이 새로고침 유지·내보내기/가져오기까지 충분히 구현 가능 |
| 2026-09-01 | 레코드 필드에 `tag`를 v2 신규 필드로 선정 | "계획" vs "실제" 구분에 바로 쓸 수 있고, 과제 카드 3(스키마 마이그레이션) 검증에도 적합 |
| 2026-09-01 | 주 경계는 월~일(월요일 00:00 시작), 시간대는 Asia/Seoul 고정 | 실제 사용자가 한국에 있고, 별도 시간대 변환 없이 로컬 날짜 문자열로 충분 |
| 2026-09-01 | 주간 집계에서 값 그룹핑 키를 `항목+단위` 조합으로 함 | 같은 항목이라도 단위가 다르면(예: 분/시간) 합산이 뒤섞이는 것을 방지 |

## 작업 로그

### 2026-09-01
- `index.html` / `style.css` / `script.js` 초안 작성 — 기록 탭, 주간 요약 탭, 데이터 관리 탭 3분할 구조
- 날짜 검증(`isValidDateStr`)과 주 경계 계산(`startOfWeek`) 단위 테스트로 확인 (2024-02-30 같은 잘못된 날짜 거부, 윤년 2월 29일은 통과)
- v1→v2 마이그레이션 함수 작성 및 재실행 안정성(idempotency) 단위 테스트로 확인
- `README.md` 초안, `검증안내서.md`·`AI_3줄.md`·`docs/*`·`CLAUDE.md`·`포트폴리오_추가용_소개글.md`로 문서 세트 분리 및 작성
- 아직 GitHub push 전 — 압축본(zip)으로만 전달됨

## 카드 5 — 5일 사용 기록 (실제 날짜만 채운다, 임의로 건너뛰지 않음)

| 일차 | 실제 날짜 | 남긴 기록 | 그날 느낀 불편 |
|---|---|---|---|
| 1일차 | (미기록) | | |
| 2일차 | (미기록) | | |
| 3일차 | (미기록) | | |
| 4일차 | (미기록) | | |
| 5일차 | (미기록) | | |

카드 5는 위 5줄을 서로 다른 실제 날짜에 채운 뒤, 반복된 불편 하나를 골라 고치고 `트러블슈팅.md`와 `AI_3줄.md`의 "AI 말을 안 들은 일"에 반영한다.

---

# T07 — 인증 붙이기 (진행 중)

## 현재 상태 요약

- 프로젝트 이름을 `plandusy-diary` → `plan-do-see-diary`로 변경
- Supabase Auth(이메일+비밀번호) 연동 코드 작성 완료 (`auth.js`, `config.js`, `script.js` 전면 개편)
- `supabase-schema.sql` 작성 완료 — `records`/`rule_changes` 테이블, RLS 정책 4+2종, 계정 삭제 RPC
- 로그인 게이트 UI 완료 (`index.html`) — 비로그인 시 로그인 화면만 보임
- `docs/07_인증구현설명서.md` ①~⑥ 전부 내용 있음 — ④는 verify_output.txt 실측 결과로 채워짐. (2) 로그아웃 후 토큰 재사용은 **미통과 확인 → 수정은 했으나 재배포·재검증 전**
- `verify.sh` 실행 완료 (2026-09-03, verify_output.txt) — 카드 2~4 증거 수집됨
- 실제 Supabase 프로젝트 생성 및 `config.js` 값 채우기 완료 — 프로젝트: `eteunidypthhlwvenyos`
- GitHub 저장소(https://github.com/kjo35861/plan-do-see-Diary, 배포 연결: https://github.com/kjo35861/plandosee) / Vercel 배포(https://plandosee-noi8.vercel.app/, 이전 전달 주소 https://plandosee-seven.vercel.app/ 와 불일치 — 재확인 필요) — 학생이 이미 준비함
- 서로 다른 실제 날짜 5일 실사용 + 규칙 변경 1건 완료 (아래 "T07 카드 5" 표 참고). 단, 1일차 질문/지표/단위 고정 문장은 아직 문서화 안 됨
- `session_is_valid()` RLS 수정 + 비밀번호 변경 기능 추가 완료(2026-09-04) — **재배포·재검증 전**

## 다음에 할 일

- [x] Supabase 프로젝트 생성 → `supabase-schema.sql` 실행 → `config.js`에 URL/anon key 반영
- [x] 로컬 또는 배포된 사이트에서 가입 2계정(A, B) 만들어 `verify.sh`로 카드 2~4 증거 수집 (2026-09-03, verify_output.txt)
- [x] `docs/07_인증구현설명서.md` ④에 실제 요청/응답 붙여넣기 (토큰은 가려서)
- [ ] **[신규]** `supabase-schema.sql`을 Supabase SQL Editor에 재실행(`session_is_valid()` 추가분 반영) → `auth.js`/`script.js`/`index.html`/`style.css` 재배포
- [ ] **[신규]** 재배포 후 `verify.sh` (2)를 다시 돌려 로그아웃 뒤 재요청 결과를 재확인하고, `docs/07_인증구현설명서.md` ④(2)·⑥의 "재검증 필요" 문구를 실제 결과로 교체
- [ ] `docs/05_배포.md`의 "T06 연결 정보" 표에 T06 최종 제출 저장소 주소·commit hash 채우기
- [ ] T06 로컬 자료가 있다면 "T06 내보내기 파일 가져오기"로 내 계정에 이전 (실제로 이전했는지 미확인)
- [x] 서로 다른 실제 날짜 5일 실사용 완료 (2026-08-27, 28, 30, 31, 09-01 — Supabase 대시보드 `created_at`으로 확인)
- [ ] 1일차 질문 한 문장·관찰 지표 1개·단위·결측치/중복/이상치/반올림/주시작요일 규칙을 문서에 문장으로 고정 (아직 어디에도 명시되어 있지 않음 — 5일 기록에 수면 시간·코딩 연습·물 섭취량이 섞여 있어 대표 지표 하나를 정해 문서화 필요)
- [x] 2일차 뒤·3일차 앞에 계획 규칙 변경 1건 기록 ("규칙 변경 기록" 폼 사용, 2026-09-03 기록)
- [ ] 5일 종료 후 `docs/07_인증구현설명서.md` ⑤⑥ 최종 다듬기, AI_3줄.md의 "AI 제안을 따르지 않은 일" 채우기

## 결정 기록 (T07)

| 날짜 | 결정 | 이유 |
|---|---|---|
| 2026-09-03 | 프로젝트명 plandusy → plan-do-see 변경 | 사용자 요청. GitHub 저장소·배포 도메인과 이름 통일 |
| 2026-09-03 | 인증 서비스로 Supabase Auth 선택 (직접구현·NextAuth 대신) | 비밀번호 해싱·세션 관리를 검증된 서비스에 맡기고, RLS로 소유자 검증을 DB 레벨에서 강제할 수 있어 정적 사이트 구조에 가장 적합했음 |
| 2026-09-03 | 소유자 검증을 애플리케이션 코드가 아닌 Postgres RLS로 구현 | 카드4 "화면에서 안 보이게 하는 것은 막은 게 아니다"라는 취지에 맞게, 서버(DB)가 강제하는 방식을 선택 |
| 2026-09-03 | 배포처를 GitHub Pages → Vercel로 변경 | 사용자가 이미 Vercel로 배포를 진행함(https://plandosee-noi8.vercel.app/) |
| 2026-09-03 | T06의 schemaVersion 마이그레이션 로직 제거, 1회성 "T06 자료 가져오기"로 대체 | 저장소가 Postgres로 바뀌면서 클라이언트 측 스키마 버전 관리가 더는 필요 없어짐 |

## 작업 로그

### 2026-09-03
- T06 폴더를 `plan-do-see-diary`로 복사, `index.html`/`style.css`/`script.js` 전면 개편(로그인 게이트, Supabase CRUD, 세션 만료 표시, 규칙 변경 로그)
- `auth.js`, `config.js`, `supabase-schema.sql`, `verify.sh` 신규 작성
- `docs/07_인증구현설명서.md` 틀 작성 (①②③⑤⑥ 초안, ④는 실측 대기)
- 기존 문서(`README`, `CLAUDE.md`, `검증안내서.md`, `AI_3줄.md`, `트러블슈팅.md`, `docs/00·01·05`) T07 기준으로 갱신, T06 AI_3줄은 `docs/archive_T06_AI_3줄.md`로 보관
- 모든 신규 JS 파일 `node --check`로 문법 검증 완료, HTML의 모든 `getElementById` 참조가 실제 id와 일치함을 스크립트로 대조
- 실제 Supabase 프로젝트 생성 및 `verify.sh` 실행 완료 (verify_output.txt) — 5개 테스트 중 4개 통과, (2) 로그아웃 후 토큰 재사용은 미통과로 확인
- 서로 다른 실제 날짜 5일(08-27, 28, 30, 31, 09-01) 실사용 및 규칙 변경 1건(물 섭취량 1.2L→1.5L, 09-03 기록) 완료

### 2026-09-04
- (2) 미통과 원인 분석: Supabase `signOut()`은 리프레시 토큰만 무효화하고, 액세스 토큰(JWT)은 자연 만료(발급 후 1시간)까지 유효 — RLS가 `auth.uid()`만 검사해서 생기는 구조적 한계였음
- `supabase-schema.sql`에 `session_is_valid()` 함수(SECURITY DEFINER, `auth.sessions` 조회) 추가, `records`/`rule_changes`의 정책 6개 전부에 `AND public.session_is_valid()` 조건 추가. 파일 전체를 재실행해도 안전하도록 모든 정책에 `drop policy if exists` 적용
- `auth.js`에 `changePassword()` 추가 — 비밀번호 변경 후 `signOut({scope:'others'})`로 다른 기기 세션 강제 종료. `index.html`/`style.css`/`script.js`에 "비밀번호 변경" 폼 연결
- 신규/수정 코드 전체 `node --check` 통과, 파일 줄바꿈(CRLF) 통일
- `docs/07_인증구현설명서.md` ③에 새 흐름 반영, ④(2)에 "미통과" 실측 결과 + "수정 반영(재검증 필요)" 안내 추가, ⑥ 갱신
- **아직 실제 Supabase 프로젝트에 반영·재검증 전** — SQL 재실행, 코드 재배포, `verify.sh` 재실행이 남음

## T07 카드 5 — 5일 사용 기록 (실제 날짜만 채운다, 임의로 건너뛰지 않음)

**1일차에 고정할 것** (한 번 정하면 5일 내내 유지):
- 답하려는 질문 한 문장: (미정)
- 관찰 지표 1개 + 단위: (미정)
- 계산 규칙(빠진 값/중복값/이상치 처리, 반올림, 주 시작 요일): (미정 — `docs/01_기획.md`의 주 시작 요일=월요일 규칙은 재사용 가능)
- 계획 규칙(바꾸기 전 초기값): (미정)

| 일차 | 실제 날짜 | 남긴 기록 | 계획 대비 실제 |
|---|---|---|---|
| 1일차 | 2026-08-27 | 물 섭취량 기록 시작 | (표 대조 미기록) |
| 2일차 | 2026-08-28 | | (표 대조 미기록) |
| **← 계획 규칙 변경 1건** | 2026-09-03 오후 5:09:16 기록 | "물 섭취량을 1.2L에서 1.5L로" — 1일차에 1.2L만 마셔서 목표를 겨우 채웠는데 실제로는 더 마시고 싶었음. 기준을 1.5L로 올려 더 현실적인 목표로 조정 | |
| 3일차 | 2026-08-30 | | (표 대조 미기록) |
| 4일차 | 2026-08-31 | | (표 대조 미기록) |
| 5일차 | 2026-09-01 | | (표 대조 미기록) |

5일 사용 소감(사용자 확인): "딱히 불편한 부분을 느끼지 못함." 화면의 합계/평균을 손으로 더한 값과 대조하는 작업(T07-C132)은 아직 안 됨 — `docs/07_인증구현설명서.md`와 `트러블슈팅.md`에 반영 필요.
