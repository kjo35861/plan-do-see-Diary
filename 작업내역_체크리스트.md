# 작업내역 체크리스트 — plandusy-diary (T06)

> 모든 AI 모델(이 프로젝트를 이어받는 어떤 세션이든)의 공용 인수인계 파일. 새 세션은 이 파일을 가장 먼저 읽는다.

## AI 프로토콜

1. 이 파일을 읽고 "진행 상황" 표에서 현재 어디까지 됐는지 파악한다.
2. 작업을 시작하기 전에 관련 문서 하나만 골라 읽는다 (`CLAUDE.md`의 3번 표 참고).
3. 작업이 끝나면 아래 "작업 로그"에 날짜와 함께 한 일을 한 줄 이상 추가한다. 코드만 고치고 여기 기록을 안 남기면 작업이 끝난 게 아니다.
4. 새로운 결정을 내렸다면(예: 기술 스택 변경) "결정 기록"에 근거와 함께 남긴다.

## 진행 상황

| 항목 | 상태 |
|---|---|
| Supabase 프로젝트 생성 및 테이블/RLS/함수 생성 | 완료 |
| 정적 HTML/JS 프런트엔드 (단일 페이지) | 완료 → 이후 분리로 대체 |
| 화면을 역할별 5개 페이지로 분리 (index/todos/logs/review/export) | 완료 |
| 문서 세트(CLAUDE.md 등) 구성 | 완료 |
| 실제 본인 계획/할 일/실행 기록 데이터 입력 | 미완료 — 사용자가 직접 해야 함 |
| GitHub 저장소 업로드 | 미완료 |
| Vercel 배포 | 미완료 |
| README에 배포 URL·소스 URL 채우기 | 미완료 |

## 작업 로그

- **초기 스키마 설계**: `plans`, `plan_revisions`, `todos`, `execution_logs`, `completion_events`, `carryover_notes` 6개 테이블과 RLS, `fn_complete_todo`/`fn_reopen_todo` 함수, 계획 수정 이력 자동 저장 트리거를 SQL로 작성해 Supabase에 반영.
- **정적 프런트엔드 1차 구현**: 단일 `index.html` + `app.js`로 계획/할 일/실행 기록/돌아보기/내보내기 기능 전부 구현.
- **RLS 정책 재실행 오류 대응**: `policy already exists` 오류 발생 → `drop policy if exists` 후 재생성하는 스크립트 제공. (자세한 내용은 `트러블슈팅.md`)
- **Supabase 키 체계 확인**: anon/service_role 레거시 키가 아니라 publishable/secret 신규 키 체계임을 최신 정보로 확인하고 안내 갱신.
- **Next.js SSR 가이드 검토 후 기각**: Supabase 대시보드가 제시한 `@supabase/ssr` + middleware 구성은 로그인 세션 관리용이라 이 과제(로그인 없음)에는 불필요하다고 판단, 정적 사이트 구조 유지로 결정.
- **화면 분리(리팩터링)**: 단일 페이지가 가독성이 떨어진다는 피드백을 받아 `index.html`(계획) / `todos.html` / `logs.html` / `review.html` / `export.html` 5개 페이지로 분리. 공통 로직은 `shared.js`로 추출, 페이지 간 "현재 선택된 계획"은 `localStorage`로 유지.
- **문서 세트 구성**: `CLAUDE.md`, 본 체크리스트, `docs/00_과제_요구사항_매핑.md`, `docs/01_기획.md`, `docs/05_배포.md`, `검증안내서.md`, `트러블슈팅.md`, `AI_3줄.md`, `포트폴리오_추가용_소개글.md` 작성.

## 결정 기록

| 결정 | 근거 |
|---|---|
| DB로 Supabase(Postgres) 채택 | 로그인 없이도 RLS로 서버 DB에 안전하게 직접 접근 가능, 별도 백엔드 서버 불필요 |
| 프런트엔드를 Next.js가 아닌 순수 HTML/CSS/JS로 채택 | 로그인/세션이 없는 과제 특성상 SSR·미들웨어가 불필요, 빌드 단계 없이 Vercel에 정적 배포 가능해 구조가 단순함 |
| 완료 중복 방지를 버튼 비활성화가 아닌 DB 함수(행 잠금)로 구현 | 과제 요구사항이 "버튼을 잠그는 것만으로 막고 있지는 않은지" 확인하라고 명시. 동시 요청에도 안전한 서버측 보장이 필요했음 |
| 화면을 5개 페이지로 분리 | 한 페이지에 모든 기능이 몰려 가독성이 떨어진다는 피드백 반영 |

## 다음 세션이 이어받을 때 먼저 할 일

1. 사용자가 실제 계획/할 일/실행 기록을 입력했는지 확인 (아직이면 카드 5 통과 기준 미충족 상태).
2. GitHub 저장소·Vercel 배포 여부 확인, 됐다면 `README.md`의 URL 자리를 채운다.
3. `검증안내서.md`대로 실제로 눌러보며 통과 여부를 확인한다.
