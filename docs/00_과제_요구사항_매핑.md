# 과제 요구사항 매핑 — T06 플랜두씨 다이어리

## A. 과제 필수 통과 기준 (과제 6 원문 T06-C## 전체)

### 카드 1 — 계획
| 코드 | 기준 | 구현 위치 |
|---|---|---|
| T06-C04 | 계획에 기간이 저장된다 | `plans.period_start`, `plans.period_end` / `index.html` |
| T06-C05 | 계획에 우선순위가 저장된다 | `plans.priority` |
| T06-C06 | 계획에 성공 기준이 저장된다 | `plans.success_criteria` |
| T06-C07 | 계획에 예상 시간이 저장된다 | `plans.estimated_minutes` |
| T06-C08 | 계획을 고쳐도 고치기 전 계획이 남아있다 | `trg_plan_before_update` 트리거 → `plan_revisions` |

### 카드 2 — 할 일
| 코드 | 기준 | 구현 위치 |
|---|---|---|
| T06-C09 | 할 일 생성 | `todos.js` 폼 제출(insert) |
| T06-C10 | 할 일 수정 | `todos.js` 수정 폼(update) |
| T06-C11 | 완료 처리 | `fn_complete_todo` RPC |
| T06-C12 | 완료 취소(되돌리기) | `fn_reopen_todo` RPC |
| T06-C13 | 삭제 | `is_deleted` 소프트 삭제 |
| T06-C14 | 마감일 저장 | `todos.due_date` |
| T06-C15 | 우선순위 저장 | `todos.priority` |
| T06-C16 | 태그 저장 | `todos.tags` (text[]) |
| T06-C17 | 예상 시간 저장 | `todos.estimated_minutes` |
| T06-C18 | 검색 | `ilike` 검색(서버 쿼리) |
| T06-C19 | 조건 필터 | 상태/우선순위 필터(서버 쿼리) |
| T06-C20 | 명시된 기준대로 정렬 | `#sort-note`에 기준 텍스트 표시 + `order()` |

### 카드 3 — 실행 기록
| 코드 | 기준 | 구현 위치 |
|---|---|---|
| T06-C23 | 시작 시각 저장 | `execution_logs.started_at` |
| T06-C24 | 끝난 시각 저장 | `execution_logs.ended_at` |
| T06-C25 | 실제 걸린 시간 저장 | `execution_logs.actual_minutes` |
| T06-C26 | 막혔던 이유 저장 | `execution_logs.blocker_reason` |
| T06-C27 | 저장해도 원래 계획 값 안 덮어씀 | 별도 테이블 적재, `plans`/`todos` UPDATE 없음 |
| T06-C21 | 완료 연타해도 완료 기록 1건 | `fn_complete_todo`의 `FOR UPDATE` 행 잠금 |
| T06-C22 | 그때 돌아보기 완료 수도 1만 증가 | 위와 동일 메커니즘 |

### 카드 4 — 돌아보기
| 코드 | 기준 | 구현 위치 |
|---|---|---|
| T06-C28 | 계획 수 = 그 계획의 안 지운 할 일 수 | `review.js` 집계 |
| T06-C29 | 완료 수 = status='done' 수 | 〃 |
| T06-C30 | 지연 수(중복 집계 방지) | KST 오늘 기준, `status!='done'` |
| T06-C31 | 막힘 수 | `blocker_reason` 존재 로그의 todo 수 |
| T06-C32 | 예상/실제/차이(단위 통일, 없으면 0) | 분 단위로 통일 합산 |
| T06-C83 | 집계 숫자 클릭 → 근거 기록 | `.stat[data-key]` 클릭 → 드릴다운 |
| T06-C33 | 고칠 점 한 줄이 다음 계획으로 이관 | `carryover_notes` → 새 계획 생성 시 자동 반영 |

### 카드 5 — 마무리
| 코드 | 기준 | 구현 위치 |
|---|---|---|
| T06-C34 | 서버 실제 DB에 저장 | Supabase Postgres |
| T06-C35 | 새로고침 후 값 복원 | 모든 화면이 매번 서버에서 재조회 |
| T06-C78 | 실제 계획 1개 이상 | 사용자가 직접 입력 (아직 미완료) |
| T06-C79 | 딸린 할 일 5개 이상 | 사용자가 직접 입력 (아직 미완료) |
| T06-C80 | 실행 기록 3개 이상 | 사용자가 직접 입력 (아직 미완료) |
| T06-C81 | 돌아보기 숫자가 전부 0이 아님 | 위 데이터 입력 후 자동 충족 |
| T06-C36 | 전체 자료 파일 내보내기 | `export.html`의 JSON 내보내기 |
| T06-C82 | 첫 화면에 공개 안내 문구 | 모든 페이지 상단 `.notice-bar` |
| T06-C58 | 비밀키 원문 노출 없음 | publishable key만 사용, secret key 미포함 |
| T06-C57 | 스크립트 문자열 그대로 표시 | `textContent`만 사용, `innerHTML` 미사용 |
| T06-C59 | 확인 방법 4줄 분리 | `README.md` / `검증안내서.md` |
| T06-C60 | AI 3줄 분리 | `AI_3줄.md` |
| T06-C01 | 시크릿 창에서 로그인 없이 열림 | 인증 기능 자체가 없음 |

## B. 학생 자체 점검 항목 (과제 문서의 "막히는 지점과 확인 순서" + 추가로 마련한 검증 장치)

- [ ] `logs.html`의 "완료 버튼 연타 테스트" 버튼으로 실제 연타 시 이벤트 1건만 남는지 직접 확인했는가
- [ ] `index.html`에서 계획을 한 번 수정한 뒤 "이력 보기"로 고치기 전 값이 남아있는지 직접 확인했는가
- [ ] `export.html`에서 다운로드한 JSON을 열어 실제 데이터가 들어있는지 확인했는가
- [ ] `<script>alert(1)</script>`를 제목에 넣어보고 알림창이 뜨지 않는지 확인했는가
- [ ] Supabase 대시보드 SQL Editor에서 `select * from pg_policies`로 RLS 정책이 6개 테이블 모두에 있는지 확인했는가
- [ ] 배포 URL을 로그인 안 된 시크릿 창에서 열어봤는가
- [ ] GitHub 저장소·배포 파일 어디에도 `sb_secret_`로 시작하는 값이 없는지 검색해봤는가 (`grep -r "sb_secret_" .`)
