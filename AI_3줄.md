# AI와 내 판단 3줄 — Plan Do See Diary (T07)

- AI에게 맡긴 일: Supabase Auth 연동 코드(가입/로그인/로그아웃/세션 유지), RLS 정책 SQL, 로그인 게이트가 있는 UI 재구성, T06 로컬 자료 → 내 계정 이전 로직, 검증용 curl 스크립트(`verify.sh`) 및 문서 세트 초안. 이후 실측(verify_output.txt)에서 "로그아웃해도 이전 액세스 토큰이 계속 통한다"(T07-C114 미통과)는 문제를 발견해, RLS가 `auth.sessions`로 세션 생존 여부까지 검사하는 `session_is_valid()` 함수와 비밀번호 변경(`changePassword`, 다른 세션 강제 종료) 기능을 추가로 맡김
- 내가 직접 판단한 일: 인증 방식(Supabase Auth) 최종 선택과 그 이유, 실제 Supabase 프로젝트 생성·배포(Vercel)·도메인 확정, curl로 직접 실행해 응답을 확인하고 문서에 옮겨 적는 일, 계획 규칙 변경 내용과 5일 사용 지표 결정
- AI 제안을 따르지 않은 일: 개발 과정에서 AI가 기존에 이미 만들어 둔 기능(구버전 화면 구조, 옛 코드 등)을 정리한다며 계속 지우려는 방향으로 제안해 왔는데, 그대로 따르지 않고 무엇을 남기고 무엇을 지울지 직접 판단해서 더 나은 방향으로 끌고 감

---
T06(플랜두씨 다이어리 1)의 AI 3줄은 [`docs/archive_T06_AI_3줄.md`](./docs/archive_T06_AI_3줄.md)에 보관.
