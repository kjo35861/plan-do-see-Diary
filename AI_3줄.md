# AI와 내 판단 — plandusy-diary

1. **AI에게 맡긴 일** — 데이터베이스 스키마 설계(테이블·관계·제약), 완료 중복 방지를 위한 Postgres 함수/트리거 작성, RLS 정책 작성, 프런트엔드(HTML/CSS/JS) 5개 페이지 전체 코드 작성, 문서 세트(README·기획·배포·트러블슈팅 등) 초안 작성.
2. **내가 직접 판단한 일** — 실제로 어떤 계획·할 일·실행 기록을 넣을지 고르는 것, DB(Supabase)를 쓰기로 한 방향 결정, 완성된 화면을 실제로 눌러보며 요구사항대로 동작하는지 확인하는 것, 배포처(Vercel)를 고른 것, 한 페이지 구조가 가독성이 떨어진다고 판단해 화면 분리를 요청한 것.
3. **AI 제안을 따르지 않은 일** — Supabase 대시보드가 자동으로 제시한 Next.js `@supabase/ssr` + middleware 세션 관리 구성은 채택하지 않음. 이유: 이 과제는 로그인 자체가 없어 세션을 유지할 필요가 없고, 정적 사이트 구조가 더 단순했기 때문. (자세한 판단 과정은 `트러블슈팅.md` 4번 참고)
