가장 먼저 `작업내역_체크리스트.md`를 읽고, 그 안의 AI 프로토콜을 따를 것.

# CLAUDE.md — plandusy-diary

## 0. 문서 세트

| 파일명 | 용도 |
|---|---|
| `README.md` | 프로젝트 개요·규칙·폴더 구조·문서 색인 |
| `작업내역_체크리스트.md` | 모든 AI 모델의 공용 인수인계 파일. 진행 상황·결정 기록·작업 로그를 여기 한 파일에 누적 기록 |
| `docs/01_기획.md` | 기술 스택·시스템(테이블/함수/트리거)·집계 알고리즘 설계 내용 |
| `docs/05_배포.md` | 배포처 비교·선택 근거·절차 |
| `docs/00_과제_요구사항_매핑.md` | 과제 필수 항목(T06-C##)과 자체 점검 항목을 구분해 정리 |
| `검증안내서.md` | 어디로 가서 무엇을 하면 무엇이 보여야 통과인지 |
| `트러블슈팅.md` | 실제로 겪은 문제·시도·비교·배운 점 |
| `AI_3줄.md` | AI에게 맡긴 일 / 내가 판단한 일 / AI 말을 안 들은 일 |
| `포트폴리오_추가용_소개글.md` | 포트폴리오에 바로 쓸 소개글 |

## 1. 세션 시작 시 가장 먼저 할 일

폴더 전체를 훑어보지 말고 `작업내역_체크리스트.md` 한 파일만 먼저 읽는다. 그 파일이 없다면 `README.md`만 읽고, 거기서 안내하는 문서를 따라간다.

## 2. 절대 통째로 읽지 말 것 (토큰 낭비)

| 경로 | 이유 |
|---|---|
| `contracts/pds-schema-v2.json` | 스키마 계약 문서. 필요한 테이블 항목만 필요 시 확인 |
| `*.zip` (예: `plandusy-diary.zip`) | 산출물 압축본. 내용 확인은 압축 풀지 말고 대응하는 원본 소스 파일을 직접 연다 |
| `node_modules/`, `.git/`, `dist/`, `build/` 등 | 빌드 산출물·의존성. 원칙적으로 읽지 않음 (이 프로젝트는 정적 사이트라 현재는 해당 없음) |

## 3. 작업 유형별로 문서 하나만 읽기

| 하려는 작업 | 읽을 문서 |
|---|---|
| 기획/설계(스키마·함수·집계 로직) 확인 | `docs/01_기획.md` |
| 배포 방법 | `docs/05_배포.md` |
| 과거 문제 해결 사례 | `트러블슈팅.md` |
| 진행 상황·다음 할 일 | `작업내역_체크리스트.md` |
| 과제 통과 기준 확인 | `docs/00_과제_요구사항_매핑.md` |
| 결과물이 통과인지 확인하는 방법 | `검증안내서.md` |

## 4. 코드 수정 시

- 어떤 파일을 고쳐야 하는지 알고 있다면 그 파일만 연다 (예: 할 일 관련이면 `todos.html`/`todos.js`만).
- 모를 때만 디렉토리 목록을 보되, 목록만 보고 내용을 전부 읽지는 않는다.
- 기존 파일을 통째로 다시 쓰지 않는다 — 필요한 부분만 수정한다.
- Supabase 스키마(테이블/함수/RLS)를 바꿀 때는 반드시 `contracts/pds-schema-v2.json`도 함께 갱신한다.

**코드를 고쳤으면 그 작업은 아직 끝난 게 아니다.** 관련 문서를 갱신해야 작업이 종료된 것으로 친다.
- `작업내역_체크리스트.md`의 작업 로그·진행사항 체크리스트에 반영
- 동작이나 구조가 바뀌었으면 `README.md`, `docs/01_기획.md`, `docs/05_배포.md` 등 해당하는 곳도 같이 수정
- "나중에 한꺼번에 정리" 하지 않는다 — 코드 변경과 문서 갱신을 같은 작업 단위로 묶는다

### 4-1. 커밋 전 문서 갱신 체크리스트

코드를 커밋하기 직전, 아래 중 해당하는 항목이 있는데 갱신을 안 했다면 **커밋하지 않는다.**

- [ ] 새 기능/동작을 추가했다 → README(또는 `docs/01_기획.md`)의 해당 설명을 갱신했는가
- [ ] 구조·파일 위치가 바뀌었다 → README의 폴더 구조·문서 색인을 갱신했는가
- [ ] 배포 방식·설정이 바뀌었다 → `docs/05_배포.md`를 갱신했는가
- [ ] 문제를 발견하고 해결했다 → `트러블슈팅.md`에 문제/시도/결과를 기록했는가
- [ ] 새로운 결정을 내렸다 → `작업내역_체크리스트.md`의 결정 기록에 추가했는가
- [ ] 어쨌든 뭔가 했다 → `작업내역_체크리스트.md`의 작업 로그에 방금 한 일을 남겼는가

체크리스트에 걸리는 게 없으면 (순수 리팩터링, 문서와 무관한 내부 정리 등) 그대로 커밋해도 된다.

## 5. 이 파일 자체 수정 규칙

새로 생긴 대형 폴더/파일이 있으면 2번 표에 한 줄 추가한다.
