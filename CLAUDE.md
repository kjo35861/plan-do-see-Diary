# CLAUDE.md — Plan Do See Diary (T06+T07)

## 0. 문서 세트

| 파일명 | 용도 |
|---|---|
| `README.md` | 프로젝트 개요·규칙·폴더 구조·문서 색인 |
| `작업내역_체크리스트.md` | **모든 AI 모델의 공용 인수인계 파일.** 진행 상황·결정 기록·작업 로그를 여기 한 파일에 누적 기록 |
| `docs/01_기획.md` | 기술 스택·데이터 구조·마이그레이션·집계 알고리즘 등 설계 내용 |
| `docs/05_배포.md` | 배포처 비교·선택 근거·절차 |
| `docs/00_과제_요구사항_매핑.md` | T06+T07 과제 카드 전부 — 필수 항목과 학생 자체 점검 항목을 구분해서 정리 |
| `docs/07_인증구현설명서.md` | T07 필수 제출물 — 인증을 무엇으로·왜·어떻게 붙였는지와 실측 확인 기록 |
| `supabase-schema.sql` | Supabase 테이블 + RLS 정책 정의 (소유자 검증의 실체) |
| `verify.sh` | 카드2~4 증거(요청/응답) 수집용 curl 템플릿 |
| `검증안내서.md` | 어디로 가서 무엇을 하면 무엇이 보여야 통과인지 |
| `트러블슈팅.md` | 실제로 겪은 문제·시도·비교·배운 점 |
| `AI_3줄.md` | AI에게 맡긴 일 / 내가 판단한 일 / AI 말을 안 들은 일 |
| `포트폴리오_추가용_소개글.md` | 나중에 개인 포트폴리오에 이 프로젝트를 추가할 때 바로 쓸 소개글 |
| `CLAUDE.md` (이 파일) | AI가 폴더를 통째로 읽지 않도록 하는 토큰 절약 규칙 |

## 0-1. 각 AI 툴에 규칙을 자동으로 읽히기

이 폴더에서 세션을 시작하면 가장 먼저 `작업내역_체크리스트.md`를 읽고, 그 안의 AI 프로토콜을 따를 것.

| 툴 | 자동으로 읽는 파일 |
|---|---|
| Claude Code | `CLAUDE.md` |
| Cursor | `.cursorrules` 또는 `.cursor/rules/` |
| Gemini CLI | `GEMINI.md` |
| (ChatGPT, DeepSeek, Kimi 등) | 자동 로드 미지원 — 대화 시작 시 "작업내역_체크리스트.md 읽고 시작해줘"라고 안내 |

폴더 전체를 훑어보지 말고, `작업내역_체크리스트.md` 한 파일만 먼저 읽는다.
그 파일이 없다면 `README.md`만 읽고, 거기서 안내하는 문서를 따라간다.

## 1. 세션 시작 시 가장 먼저 할 일

1. `작업내역_체크리스트.md`를 읽고 마지막 작업 로그와 "다음에 할 일"을 확인한다.
2. `docs/00_과제_요구사항_매핑.md`에서 어느 카드까지 통과 기준을 충족했는지 확인한다.
3. 카드 5(5일 실사용) 진행 중이라면, 실제 사용 날짜가 며칠째인지 체크리스트에서 확인한다.
4. T07 작업 중이라면 `docs/07_인증구현설명서.md`의 ④(실측 확인 기록)가 채워졌는지 확인한다 — 비어 있으면 아직 미완이다.

## 2. 절대 통째로 읽지 말 것 (토큰 낭비)

| 경로 | 이유 |
|---|---|
| `*.zip` (배포용 압축본) | 산출물 압축본. 개별 소스 파일을 대신 읽는다 |
| `script.js`의 마이그레이션/집계 로직 재확인 시 | 함수 단위로만 열어본다 — 전체 재작성 금지, `str_replace`로 필요한 부분만 수정 |

이 프로젝트는 현재 파일 수가 적어(HTML/CSS/JS 3개 + 문서) 대량 폴더는 없다. 새로 생기면 이 표에 추가한다.

## 3. 작업 유형별로 문서 하나만 읽기

| 하려는 작업 | 읽을 문서 |
|---|---|
| 기획/설계 확인 (데이터 구조, 알고리즘) | `docs/01_기획.md` |
| 배포 방법 | `docs/05_배포.md` |
| 과거 문제 해결 사례 | `트러블슈팅.md` |
| 진행 상황·다음 할 일 | `작업내역_체크리스트.md` |
| 과제 카드별 통과 기준 확인 | `docs/00_과제_요구사항_매핑.md` |
| 인증 설계 근거·소스 위치 확인 | `docs/07_인증구현설명서.md` |
| RLS/테이블 구조 확인 | `supabase-schema.sql` |

## 4. 코드 수정 시

- 어떤 파일을 고쳐야 하는지 알고 있다면 그 파일만 연다 (`index.html` / `style.css` / `script.js` / `auth.js` / `config.js` 중 하나를 이름으로 지정).
- `supabase-schema.sql`을 고칠 때는 실제 Supabase 프로젝트의 SQL Editor에도 같은 변경을 반영해야 한다는 점을 `작업내역_체크리스트.md`에 남긴다 (파일을 고친 것만으로는 배포된 DB가 바뀌지 않음).
- 기존 파일을 통째로 다시 쓰지 않는다 — `str_replace`로 필요한 부분만 수정한다.
- **코드를 고쳤으면 그 작업은 아직 끝난 게 아니다.** 관련 문서를 갱신해야 작업이 종료된 것으로 친다.
  - `작업내역_체크리스트.md`의 작업 로그·진행사항 체크리스트에 반영
  - 동작이나 구조가 바뀌었으면 `README.md`, `docs/01_기획.md`, `docs/05_배포.md` 중 해당하는 곳도 같이 수정
  - "나중에 한꺼번에 정리" 하지 않는다 — 코드 변경과 문서 갱신을 같은 작업 단위로 묶는다

### 4-1. 커밋 전 문서 갱신 체크리스트

- [ ] 새 기능/동작을 추가했다 → README(또는 `docs/01_기획.md`)의 해당 설명을 갱신했는가
- [ ] 구조·파일 위치가 바뀌었다 → README의 폴더 구조·문서 색인을 갱신했는가
- [ ] 배포 방식·설정이 바뀌었다 → `docs/05_배포.md`를 갱신했는가
- [ ] 문제를 발견하고 해결했다 → `트러블슈팅.md`에 문제/시도/결과를 기록했는가
- [ ] 새로운 결정을 내렸다 → `작업내역_체크리스트.md`의 결정 기록에 추가했는가
- [ ] 어쨌든 뭔가 했다 → `작업내역_체크리스트.md`의 작업 로그에 방금 한 일을 남겼는가

체크리스트에 걸리는 게 없으면 (순수 리팩터링 등) 그대로 커밋해도 된다.

## 5. 이 파일 자체 수정 규칙

새로 생긴 대형 폴더/파일이 있으면 2번 표에 한 줄 추가한다. 규칙이 과하다 싶으면 사용자와 상의 후 줄인다.
