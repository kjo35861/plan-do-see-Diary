# 짧은 확인 방법 — Plan Do See Diary (T07)

① 어디로 가나요
https://plandosee-noi8.vercel.app/ 를 새 시크릿(비공개) 창에서 엽니다. 계정을 만들지 않아도 로그인 화면까지는 보입니다.

② 세 단계 안에 무엇을 하나요
1. "가입" 탭에서 새 이메일/비밀번호로 가입합니다(실제 존재하지 않는 이메일이어도 확인 메일 발송 여부만 다를 뿐 계정은 생성됩니다).
2. 로그인 후 "기록" 탭에서 `가상 기록 5건 채우기`를 누르고, 아무 행을 수정·삭제해봅니다.
3. `로그아웃`을 누른 뒤 같은 브라우저에서 뒤로가기를 눌러 자료 화면에 다시 접근해봅니다.

③ 무엇이 보이면 통과인가요
- 로그인 없이 접속하면 첫 화면이 로그인 화면이다.
- 가입 → 로그인 → 기록 CRUD → 로그아웃이 화면 이동 없이 한 페이지 안에서 모두 된다.
- 로그아웃 뒤에는 다시 로그인 화면으로 돌아가고, 이전 화면의 기록은 보이지 않는다.
- "데이터 관리" 탭에서 로그인 계정 이메일과 세션 만료 시각이 표시된다.
- "데이터 관리" 탭에서 비밀번호를 바꿀 수 있고, 바꾸면 이 기기는 로그인이 유지된 채 다른 기기·브라우저의 로그인만 끊긴다.

**④ 안 될 때는 무엇이 보이나요
- 이미 가입된 이메일로 다시 가입을 시도하면 "이미 가입된 이메일입니다" 메시지가 뜬다.
- 아이디가 없거나 비밀번호가 틀리면 두 경우 모두 "이메일 또는 비밀번호가 올바르지 않습니다"로 동일하게 표시된다(어느 쪽이 틀렸는지 구분해서 알려주지 않음).
- 세션이 만료된 상태로 기록을 저장/조회하려 하면 자동으로 로그인 화면으로 돌아간다.
- 새 비밀번호를 6자 미만으로 넣으면 저장되지 않고 오류 메시지가 뜬다.

> **참고**: 이전 실측(2026-09-03)에서 "로그아웃 뒤 같은 토큰으로 재요청하면 거절된다"는 항목이 미통과였다(액세스 토큰이 자연 만료 전까지 유효). `supabase-schema.sql`에 `session_is_valid()` 검사를 추가해 고쳤고, 2026-09-04 재검증에서 실제로 거절됨(200+빈 배열)을 확인했다. 자세한 내용은 `docs/07_인증구현설명서.md` ④(2) 참고.

---

## (참고) T06과의 관계

T07은 T06(플랜두씨 다이어리 1)에 이어 붙인 것입니다. T06에서 만든 로컬(localStorage) 자료는 "데이터 관리" 탭의 `T06 내보내기 파일 가져오기`로 로그인한 내 계정으로 옮길 수 있습니다.

과제 통과 기준(T07-C77, C78)은 "T07 결과물이 이어서 사용한 최종 T06 결과를 명시"할 것을 요구합니다 — T06을 제출했던 저장소 주소와, 그 저장소에서 T06 최종 제출로 삼았던 커밋 해시를 `docs/05_배포.md`의 "T06 연결 정보" 표에 채워 넣습니다.
