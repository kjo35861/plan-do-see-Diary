# 검증 안내서 — plandusy-diary

각 화면별로 "어디로 가나요 / 무엇을 하나요 / 무엇이 보이면 통과인가요 / 안 될 때는 무엇이 보이나요"를 정리했다.

## 1. 계획 (`index.html`)

- 어디로 가나요 — 배포 주소 루트(`/`).
- 무엇을 하나요 — 계획을 하나 만들고, "수정"으로 성공 기준을 한 글자 바꿔 저장한다.
- 무엇이 보이면 통과인가요 — 목록에 새 계획이 뜨고, "이력 보기"를 누르면 방금 고치기 전의 원래 값이 남아 있다.
- 안 될 때는 무엇이 보이나요 — "이력 보기"를 눌러도 목록이 비어 있거나, 콘솔에 Supabase 오류가 뜬다.

## 2. 할 일 (`todos.html`)

- 어디로 가나요 — 상단 내비게이션의 "할 일".
- 무엇을 하나요 — 할 일을 5개 이상 만들고, 그중 하나를 "완료"로 바꾼 뒤 "되돌리기"를 눌러본다. 검색창에 제목 일부를 입력해본다.
- 무엇이 보이면 통과인가요 — 완료 시 상태 배지가 "완료"로 바뀌고, 되돌리면 다시 "진행 중"이 된다. 검색어에 맞는 항목만 표에 남는다.
- 안 될 때는 무엇이 보이나요 — 상단 배지에 "선택된 계획이 없습니다" 안내만 뜨고 폼 자체가 안 보인다 → 먼저 "계획" 화면에서 "이 계획 선택"을 눌러야 한다.

## 3. 실행 기록 (`logs.html`)

- 어디로 가나요 — 상단 내비게이션의 "실행 기록".
- 무엇을 하나요 — 대상 할 일을 고르고 실행 기록을 하나 저장한다. 그다음 "완료 버튼 연타 테스트"를 누른다.
- 무엇이 보이면 통과인가요 — 실행 기록이 표에 뜨고, 연타 테스트 결과가 "완료 이벤트 기록 수: 1건"으로 표시된다.
- 안 될 때는 무엇이 보이나요 — 연타 테스트 결과가 2건 이상이면 중복 방지가 깨진 것 → `docs/01_기획.md`의 `fn_complete_todo` 로직을 다시 확인.

## 4. 돌아보기 (`review.html`)

- 어디로 가나요 — 상단 내비게이션의 "돌아보기".
- 무엇을 하나요 — 대상 계획을 고르고 "집계 계산"을 누른 뒤, 숫자 카드 중 하나(예: "완료 수")를 클릭한다.
- 무엇이 보이면 통과인가요 — 숫자 아래에 그 숫자를 구성한 실제 할 일 목록이 펼쳐진다. "다음 계획으로 넘길 한 줄"을 저장하면 "계획" 화면에서 새 계획을 만들 때 성공 기준 앞에 자동으로 붙는다.
- 안 될 때는 무엇이 보이나요 — 숫자가 전부 "-"나 0으로만 남아있다 → 해당 계획에 할 일/실행 기록이 아직 없는 것.

## 5. 내보내기 (`export.html`)

- 어디로 가나요 — 상단 내비게이션의 "내보내기".
- 무엇을 하나요 — "전체 자료 내보내기" 버튼을 누른다.
- 무엇이 보이면 통과인가요 — `plandusy-diary-export-YYYY-MM-DD.json` 파일이 다운로드되고, 열어보면 실제로 입력한 계획/할 일/실행 기록이 들어있다.
- 안 될 때는 무엇이 보이나요 — 파일이 다운로드되지 않거나 각 테이블 배열이 빈 배열(`[]`)로만 채워져 있다.

## 공통 확인

- 아무 페이지나 새로고침(F5)해도 방금 입력한 값이 그대로 남아있으면 통과(서버 DB 저장 확인).
- 아무 페이지나 로그인 요구 없이 시크릿 창에서 바로 열리면 통과.
