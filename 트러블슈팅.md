# 트러블슈팅 — 플랜두씨 다이어리

## 1. `Date` 객체가 잘못된 날짜를 자동으로 보정해버림

**문제** — `new Date(2024, 1, 30)`처럼 존재하지 않는 날짜(2월 30일)를 만들면 JS는 에러를 내지 않고 3월 2일로 자동 보정한다. 단순히 "파싱이 되는가"만 검사하면 잘못된 날짜가 유효한 것으로 통과해버려, 카드 4의 "잘못된 날짜는 집계에 섞이지 않는다" 기준을 충족하지 못한다.

**시도** — 정규식(`YYYY-MM-DD` 형식)만으로 검사 → 형식은 맞지만 존재하지 않는 날짜(2월 30일 등)는 여전히 통과함.

**해결** — `Date` 객체를 만든 뒤, 그 객체의 `getFullYear()/getMonth()/getDate()`가 입력했던 연/월/일과 정확히 같은지 재확인. 다르면(=보정이 일어났으면) 잘못된 날짜로 판정.

**검증** — `node`로 단위 테스트: `2024-02-30` → false, `2024-02-29`(윤년) → true, `2023-02-29`(평년) → false. 의도대로 동작함을 확인.

## 2. 스키마 마이그레이션을 두 번 실행해도 안전해야 함

**문제** — 카드 3 통과 기준 중 "변환을 두 번 실행해도 기록이 늘거나 다시 바뀌지 않습니다"를 만족해야 하는데, 단순히 "레코드를 순회하며 `tag` 필드를 추가"하는 방식은 재실행해도 결과적으로 안전하긴 하지만, 코드만 보고는 안전성을 확신하기 어려웠다.

**해결** — 마이그레이션 함수가 각 레코드에 대해 `tag` 필드 존재 여부를 개별적으로 검사해 이미 있으면 건드리지 않도록 작성(`Object.prototype.hasOwnProperty`). 그 결과 `wasLegacy` 플래그도 "실제로 무언가 바뀌었는가"를 정확히 반영하게 됨.

**검증** — `node`로 단위 테스트: v1 배열 2건을 1차 변환(→ `wasLegacy: true`, tag 추가됨) 후 그 결과를 다시 변환(→ `wasLegacy: false`, 레코드 수·ID 동일)하는 것을 확인.

## 3. GitHub 저장소 push를 Claude가 대신 할 수 없음

**문제** — 이전 T03·T04 과제처럼 이번에도 새 GitHub 저장소를 만들고 GitHub Pages로 배포하려 했으나, 이 작업 환경에는 `gh` CLI가 없고 `git config`에도 인증 정보가 없어 직접 push가 불가능했다.

**해결** — 코드를 완성해 압축 파일(zip)로 전달하고, `docs/05_배포.md`에 학생이 직접 실행할 `git init → remote add → push` 절차를 정리해 넘김. push 완료 후 배포 주소가 확정되면 문서들의 URL 자리를 채우기로 함.

## (진행 예정) 카드 5 — 5일 실사용 중 발견한 불편

카드 1~4를 마친 뒤 서로 다른 실제 날짜 5일에 걸쳐 사용하며 반복된 불편을 여기에 기록한다. (아직 미시작 — `작업내역_체크리스트.md`의 "카드 5 — 5일 사용 기록" 표 참고)

---

## T07 — 인증을 붙이며 겪은 것

### 4. RLS는 "403 거부"가 아니라 "존재를 감추는 방식"으로 동작함

**문제** — 과제 통과 기준(T07-C121)은 "403으로, 또는 존재 자체를 감추는 404로" 거절되어야 한다고 명시한다. Supabase RLS(PostgREST)는 기본적으로 권한 없는 행에 대해 명시적인 403/404 에러 대신, **그냥 그 행이 없는 것처럼 빈 결과(200 + 빈 배열, 또는 영향 0건)**를 돌려준다.

**해결** — 이것도 과제가 허용한 두 방식 중 후자("존재를 감추는" 방식)에 해당한다는 점을 `docs/07_인증구현설명서.md`에 명시했다. 대신 애플리케이션 코드(`script.js`)에서 UPDATE/DELETE 응답의 영향받은 행 수가 0건이면 "내 기록이 아니거나 이미 삭제됨"이라는 안내를 사용자에게 보여주도록 방어 코드를 추가했다 — 그렇지 않으면 사용자 입장에서는 아무 반응이 없어 보여 오류처럼 느껴질 수 있었다.

### 5. anon key를 프런트엔드에 그대로 노출해도 되는지 혼동

**문제** — "비밀키가 브라우저 코드에 없어야 한다(T07-C113)"는 기준을 보고 처음엔 Supabase의 `anon` key도 숨겨야 하나 고민했다.

**해결** — Supabase의 설계상 `anon` key는 애초에 브라우저에 노출되는 것을 전제로 만들어졌고(공개 키), 실제 접근 제어는 RLS가 담당한다. 숨겨야 하는 것은 `service_role`(관리자) 키뿐이며, 이 프로젝트의 프런트엔드 코드 어디에도 `service_role` 키를 넣지 않았다. `config.js` 상단에 이 구분을 주석으로 명시해 두었다.

### (진행 예정) 5일 실사용 중 계획 규칙 변경

카드 5의 5일 사용과 3일차 전 규칙 변경은 아직 실행 전이다. `작업내역_체크리스트.md`의 T07 5일 사용 기록표에 진행 상황을 남긴다.
