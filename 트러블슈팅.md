# 트러블슈팅 — plandusy-diary

## 1. RLS 정책 재실행 시 "policy already exists" 오류

- **문제**: 최초 SQL 스크립트를 다시 실행하니 `ERROR: 42710: policy "public read plans" for table "plans" already exists` 발생.
- **원인**: `create policy`는 같은 이름의 정책이 이미 있으면 실패하는데, 스크립트에 `if not exists` 처리가 없었음.
- **시도/해결**: 6개 테이블의 모든 정책에 대해 `drop policy if exists ... ; create policy ...;` 쌍으로 다시 실행하는 스크립트를 제공해 해결.
- **배운 점**: 재실행 가능성이 있는 DDL 스크립트는 처음부터 `drop ... if exists` 또는 (Postgres 15+라면) `create policy if not exists` 형태로 작성해두면 재작업 비용이 준다.

## 2. Supabase Project URL에 `/rest/v1/`을 포함해서 넣은 실수

- **문제**: 사용자가 대시보드의 "API URL" 칸에 표시된 `https://<project>.supabase.co/rest/v1/`을 그대로 `NEXT_PUBLIC_SUPABASE_URL`에 넣으려 함.
- **원인**: `createClient()`가 내부적으로 `/rest/v1/`을 자동으로 붙이기 때문에, 이미 경로가 붙은 값을 넣으면 요청 경로가 중복됨.
- **해결**: `config.js`에는 프로젝트 기본 주소(`https://<project>.supabase.co`)만 넣도록 안내.
- **배운 점**: Supabase 대시보드가 보여주는 값을 그대로 복사하기보다, 어떤 SDK 함수에 어떤 형태의 값이 필요한지 먼저 확인해야 한다.

## 3. Supabase API 키 체계 변경(anon/service_role → publishable/secret)

- **문제**: 처음에는 anon/service_role 기준으로 안내했으나, 실제 대시보드에는 이미 새 프로젝트가 publishable/secret 키만 발급하는 상태였음.
- **원인**: Supabase가 2025년 말부터 legacy JWT 키(anon/service_role)를 신규 키(`sb_publishable_...`/`sb_secret_...`)로 단계적으로 전환 중.
- **해결**: 최신 정보를 다시 검색해 새 키 체계 기준으로 안내를 갱신. 기능적으로는 동일하게 낮은 권한/높은 권한 역할을 그대로 대체.
- **배운 점**: 외부 서비스의 대시보드 UI/키 체계는 시점에 따라 달라질 수 있으므로, 학습 시점의 지식보다 실제 대시보드 화면을 우선 신뢰해야 한다.

## 4. Next.js `@supabase/ssr` 가이드가 이 프로젝트에는 맞지 않음

- **문제**: Supabase 대시보드의 "Connect" 안내가 Next.js + 쿠키 기반 세션(`@supabase/ssr`, middleware) 구성을 자동으로 보여줌. 이걸 그대로 따라야 하는지 혼란이 있었음.
- **원인**: 그 가이드는 로그인 세션을 서버에서 유지해야 하는 앱을 전제로 한 것. 이 과제는 애초에 로그인이 없음.
- **해결**: 세션 관리가 필요 없다는 점을 확인하고, 정적 HTML/JS + 브라우저에서 직접 publishable key로 호출하는 기존 구조를 유지하기로 결정. (근거는 `작업내역_체크리스트.md`의 결정 기록 참고)
- **배운 점**: 플랫폼이 기본 제공하는 스타터 코드가 항상 내 프로젝트 조건(로그인 유무 등)에 맞는 것은 아니므로, 그대로 따르기 전에 내 요구사항과 대조해야 한다.

## 5. 완료 버튼 연타 시 중복 기록 방지

- **문제**: 완료 버튼을 연달아 두 번 눌러도 완료 기록이 1건만 남아야 하는데, 클라이언트에서 버튼만 비활성화하는 방식은 네트워크 지연 시 동시에 두 요청이 나갈 수 있어 근본적인 해결이 아님.
- **해결**: Postgres 함수(`fn_complete_todo`) 안에서 `SELECT ... FOR UPDATE`로 해당 할 일 행을 잠그고, `status`가 `'active'`일 때만 `'done'`으로 바꾸고 `completion_events`에 삽입하도록 구현. 이미 `done`이면 아무 것도 하지 않음.
- **배운 점**: "두 번 눌러도 한 번만 반영"류의 요구사항은 클라이언트 방어만으로는 불충분하고, DB 트랜잭션/행 잠금 같은 서버측 보장이 필요하다.

## 6. 한 페이지에 모든 기능이 몰려 가독성 저하

- **문제**: 계획/할 일/실행 기록/돌아보기/내보내기가 전부 `index.html` 한 페이지에 있어 화면이 길고 읽기 불편하다는 피드백.
- **해결**: 역할별로 `index.html`(계획) / `todos.html` / `logs.html` / `review.html` / `export.html` 5개 페이지로 분리하고, 공통 로직은 `shared.js`로 추출. 페이지 간 "현재 선택된 계획"은 `localStorage`로 유지해 이동해도 문맥이 끊기지 않게 함.
- **배운 점**: 기능이 늘어날수록 단일 페이지 구조는 빠르게 만들 수 있지만 가독성이 떨어지므로, 역할 단위 분리와 공통 모듈 추출을 함께 고려해야 한다.
